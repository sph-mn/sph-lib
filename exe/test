#!/usr/bin/guile
!#

(import (sph) (sph test) (sph test report) (sph alist))

(define settings
  (alist-quoted-merge-key/value test-settings-default path-prefix "modules"))

(test-report-compact (test-execute-modules-by-prefix #:settings settings (q (test module sph))))
